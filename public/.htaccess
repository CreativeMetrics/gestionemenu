RewriteEngine On

# Se il file/cartella richiesto esiste davvero (assets, uploads), servilo direttamente.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Altrimenti tutto passa dal front controller.
RewriteRule ^ index.php [L]
